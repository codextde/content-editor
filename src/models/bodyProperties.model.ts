import { IContentItemProperty } from './contentItemProperty.model';

export interface IBodyProperties {
    styles?: any;
    background?: any;
    padding?: any;
    css?: string;
    direction?: string;
}
